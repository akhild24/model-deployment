apiVersion: v1
kind: Service
metadata:
  name: ml-serving-svc
  namespace: ml-platform
  labels:
    app: ml-serving
spec:
  type: ClusterIP
  selector:
    app: ml-serving
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
